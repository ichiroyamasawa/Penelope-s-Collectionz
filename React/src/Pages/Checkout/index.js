import React from "react";
import CheckoutForm from "./../../Components/CheckoutForm";

const Checkout = () => {
  return (
    <div>
      <CheckoutForm />
    </div>
  );
};

export default Checkout;
